# GPU/RunPod dependencies.
# NOTE: torch + torchvision are intentionally omitted — install the CUDA build
# from the PyTorch index (see setup_runpod.sh) or use a RunPod PyTorch base
# image that already ships them. Installing plain `torch` here would pull the
# CPU wheel and silently disable the GPU.

numpy
scipy
pandas
scikit-learn>=1.3
Pillow
opencv-python-headless     # headless: no GUI libs needed on a server
matplotlib
tqdm
PyYAML

ultralytics
transformers
safetensors

umap-learn                 # GPU profile uses UMAP for the 2-D projection
